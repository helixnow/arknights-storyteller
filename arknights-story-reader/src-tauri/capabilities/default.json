{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window on every target platform",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
